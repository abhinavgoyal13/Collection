/*
package JavaBasics.LinkedList;

public class RemoveDuplicates {

    public void removeDuplcate(){
        if( (head.next!=null) && (head.val== head.next.val))
        {
            ////    current= current.next;
            //flag=1;
            head= head.next;
            current= current.next;
            while((current.next!=null) && head.val==head.next.val)
            {
                head= head.next;
                current=current.next;

            }
            head= head.next;
            current=current.next;
            continue;
        }
        else
        {

            prev.next=current;

            System.out.println(prev.val);
            current= current.next;next
            //prev = prev.next;



        }
        head= head.next;
    }
        return prev1;
    }
}
*/
